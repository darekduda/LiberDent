@model IEnumerable<LiberDent.Data.Data.CMS.ONas>

@{
    ViewData["Title"] = "Lista";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h6>Lista</h6>

<p>
    <a class="btn btn-outline-success" asp-action="Create">Utwórz nowy</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naglowek)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tresc1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tresc2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Naglowek)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tresc1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tresc2)
                </td>
                <td>
                    <a class="btn btn-outline-warning" asp-action="Edit" asp-route-id="@item.IdONas">Edytuj</a> |
                    <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@item.IdONas">Szczegóły</a> |
                    <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@item.IdONas">Usuń</a>
                </td>
            </tr>
        }
    </tbody>
</table>
